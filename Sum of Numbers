#include <iostream>
using namespace std;

int main(){
int n;
cout << "Enter an Positive number,ONLY POSITIVE " << endl;
cin >> n;

int f,a;

	for (a = 0; a <= n; a++){

		if (a == 0)
		f= 1;
		else
		f = f * a;
}
	cout << "Factorial: "<< f << endl;
return 0;
}
